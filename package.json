{
  "name": "proxy",
  "version": "1.0.0",
  "main": "frontend/main.jsx",
  "scripts": {
    "start": "node frontend/main.jsx",
    "build": "cd frontend && npm install && npm run build",
    "heroku-postbuild": "npm run build"
  },
  "dependencies": {
    "express": "^4.21.2",
    "http-proxy-middleware": "^2.0.6"
  },
  "engines": {
    "node": ">=16.x"
  }
}
