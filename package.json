{
  "name": "proxy",
  "version": "1.0.0",
  "main": "main.jsx",
  "scripts": {
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=16.x"
  }
}
